import { Injectable } from '@angular/core';
import { PlotterService } from './plotter.service';

@Injectable()
export class StatusService {

  constructor() {

  }

  setMessage(message: string, fadeOut: boolean = false ) {
  }
}
